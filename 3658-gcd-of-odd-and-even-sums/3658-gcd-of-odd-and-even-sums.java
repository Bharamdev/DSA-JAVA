class Solution {
    public int gcdOfOddEvenSums(int n) {
        // int oddSum=0,evenSum=0;
        // for(int i=0, even=0; i<n; i++, even+=2) evenSum+=even;
        // for(int i=0, odd=1; i<n; i++, odd+=2) oddSum+=odd;
        // int ans=1;
        // for(int i=1; i<= Math.max(evenSum, oddSum);i++){
        //     if(evenSum%i==0 && oddSum%i==0) ans=i;
        // }
        // return ans;
        return n;
    }
}